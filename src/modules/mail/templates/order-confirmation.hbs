<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body { font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif; line-height: 1.6; color: #333; background-color: #f4f4f4; margin: 0; padding: 0; }
        .container { max-width: 600px; margin: 20px auto; background: #fff; border-radius: 8px; overflow: hidden; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
        .header { background-color: #4CAF50; color: #fff; padding: 20px; text-align: center; }
        .header h1 { margin: 0; font-size: 24px; }
        .content { padding: 20px; }
        .order-info { margin-bottom: 20px; border-bottom: 2px solid #eee; padding-bottom: 10px; }
        .order-info p { margin: 5px 0; }
        .items-table { width: 100%; border-collapse: collapse; margin-bottom: 20px; }
        .items-table th, .items-table td { padding: 12px; text-align: left; border-bottom: 1px solid #ddd; }
        .items-table th { background-color: #f8f9fa; color: #555; }
        .total-section { text-align: right; margin-top: 20px; }
        .total-row { display: flex; justify-content: space-between; margin-bottom: 5px; }
        .final-total { font-size: 18px; font-weight: bold; color: #4CAF50; border-top: 2px solid #eee; padding-top: 10px; }
        .footer { background-color: #333; color: #fff; text-align: center; padding: 15px; font-size: 12px; }
        .address-box { background: #f9f9f9; padding: 15px; border-radius: 5px; margin-top: 20px; }
    </style>
</head>
<body>
<div class="container">
    <div class="header">
        <h1>Cảm ơn bạn đã đặt hàng!</h1>
    </div>
    <div class="content">
        <p>Xin chào <strong>{{name}}</strong>,</p>
        <p>Đơn hàng của bạn đã được tiếp nhận và đang được xử lý.</p>

        <div class="order-info">
            <p><strong>Mã đơn hàng:</strong> #{{orderId}}</p>
            <p><strong>Ngày đặt:</strong> {{createdAt}}</p>
        </div>

        <table class="items-table">
            <thead>
            <tr>
                <th>Sản phẩm</th>
                <th>SL</th>
                <th>Đơn giá</th>
            </tr>
            </thead>
            <tbody>
            {{#each items}}
                <tr>
                    <td>{{this.productName}}
                        {{#if this.productVariant}}
                            <br><small style="color: #888;">({{this.productVariant.name}})</small>
                        {{/if}}
                    </td>
                    <td>{{this.quantity}}</td>
                    <td>{{this.priceFormatted}}</td>
                </tr>
            {{/each}}
            </tbody>
        </table>

        <div class="total-section">
            <div class="total-row">
                <span>Tạm tính:</span>
                <span>{{totalAmount}}</span>
            </div>
            <div class="total-row">
                <span>Phí vận chuyển:</span>
                <span>{{shippingFee}}</span>
            </div>
            <div class="total-row">
                <span>Giảm giá:</span>
                <span>-{{discountAmount}}</span>
            </div>
            <div class="total-row final-total">
                <span>Tổng cộng:</span>
                <span>{{finalAmount}}</span>
            </div>
        </div>

        <div class="address-box">
            <h3 style="margin-top: 0;">Thông tin giao hàng</h3>
            <p>📍 {{shippingAddress}}</p>
            <p>📞 {{phoneNumber}}</p>
        </div>
    </div>
    <div class="footer">
        <p>&copy; 2024 Your E-Commerce Store. All rights reserved.</p>
    </div>
</div>
</body>
</html>